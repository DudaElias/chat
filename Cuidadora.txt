
public class CuidadoraDeUsuario
{
	public cuidadoraDeUsuario (Socket conexao, salas salas) throws Exception
	{
	//declarar e instanciar OOS e OIS;
	//interagir com o usr via OOS e OIS para descobrir o nome da sala em que ele deseja estar;
	//procurar em salas a sala com o nome desejado;
	//interagir com usr via OOS e OIS para descobir o nome que ele deseja usar, eventualmente cheia
	//incluir o usuario na sala
	}

	public void run()
	{
		...
		for(;;)
		{	
		}
	}
}